import { HttpService.Interceptor } from './http-service.interceptor';

describe('HttpService.Interceptor', () => {
  it('should create an instance', () => {
    expect(new HttpService.Interceptor()).toBeTruthy();
  });
});
